# Vendored libraries must keep their exact bytes: with core.autocrlf=true git would rewrite their line endings,
# and the hashes recorded in web/vendor/*/SOURCE.md would no longer match.
web/vendor/** -text
*.wasm binary
